{
    "title": "Invalid Dataset Input - cannot contain spaces",
    "queryString": "In courses dataset two entries, find all entries; show Audit; sort in ascending order by Audit.",
    "response": {
        "code": 400,
        "body": {
            "error": ""
        }
    },
    "queryAST": {
        "ID": "two entries",
        "KIND": "two entries",
        "WHERE": {},
        "OPTIONS": {
            "COLUMNS": ["two entries_audit"],
            "ORDER": "two entries_audit"
        }
    }
}
